const string = ['a', 'b', 'c', 'd', 'e']

//push
string.push('z'); //0(1)

//pop
string.pop(); // 0(n)

//unshift

string.unshift('A'); // 0(n)

//splice

string.splice(2, 0, 'alien'); // 0(n)

console.log(string);